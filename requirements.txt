# Project Sera - Dependency List
# Install with: pip install -r requirements.txt

# --- UI Framework ---
# PySide6 is used instead of PyQt6 because it is LGPL-licensed, which is
# free to use in closed-source commercial/internal tools. PyQt6 requires
# a paid commercial license for that use case.
# Version left unpinned (floor only) so pip can pick whatever build is
# compatible with your installed Python version.
PySide6>=6.7
qtawesome>=1.3.1

# --- Encrypted SQLite (SQLCipher bindings) ---
# Prebuilt Windows/Mac/Linux wheels, no manual OpenSSL/SQLCipher compile needed.
sqlcipher3-wheels>=0.5.3

# --- Browser Automation ---
playwright>=1.46

# --- Security / Key Derivation ---
argon2-cffi>=23.1      # Admin PIN hashing (Argon2id)
cryptography>=43.0     # PBKDF2 key derivation for the DB encryption key

# --- VSDC visual capture (OS-level screen + accessibility reads) ---
# Without these the app still installs and launches, but VSDC captures nothing:
# PyInstaller only WARNS about a missing hidden import, it does not fail the build.
Pillow>=10.0                            # window screenshots and crops fed to OCR
comtypes>=1.4                           # Windows UI Automation: VSDC-X exact text + address-bar URL
winrt-runtime>=3.2                      # Windows.Media.Ocr bindings for legacy VSDC OCR
winrt-Windows.Foundation>=3.2
winrt-Windows.Foundation.Collections>=3.2
winrt-Windows.Graphics.Imaging>=3.2
winrt-Windows.Media.Ocr>=3.2
winrt-Windows.Storage.Streams>=3.2

# --- Data processing ---
pandas>=2.0
numpy>=1.26
lxml>=5.0

# --- Packaging ---
pyinstaller>=6.10

# FST Classifier and FST Tracer Alpha Excel report generation
openpyxl>=3.1.2
